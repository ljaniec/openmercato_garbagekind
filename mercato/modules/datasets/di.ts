import type { AppContainer } from '@open-mercato/shared/lib/di/container'

/** Reguły pochodzenia siedzą w `lib/lineage.ts` jako czyste funkcje. */
export function register(_container: AppContainer): void {}
