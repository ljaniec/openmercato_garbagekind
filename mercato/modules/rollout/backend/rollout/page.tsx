import { Page, PageBody } from '@open-mercato/ui/backend/Page'
import RolloutBoard from '../../components/RolloutBoard'

export default function RolloutPage() {
  return (
    <Page>
      <PageBody>
        <RolloutBoard />
      </PageBody>
    </Page>
  )
}
